import { cn } from "@/lib/utils";
import { type VariantProps, cva } from "class-variance-authority";
import { AlertTriangle } from "lucide-react";
export const Banner = () => {
	return <div className="bg-blue-500">Banner</div>;
};
